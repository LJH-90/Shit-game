# MolGam (몰래 게임) — 실행 계획

회사 PC 화면 위에 투명 레이어로 떠서 동작하는 메탈슬러그풍 런앤건.
핫키 한 번에 사라지고, 다시 한 번에 그 자리에서 이어집니다.

작성일: 2026-09-13 · 상태: 계획 확정 대기

---

## 1. 확정 사항 (인터뷰 결과)

| 항목 | 결정 |
|---|---|
| 기술 기반 | IconCourt 방식 재사용 — Python 표준 라이브러리(`tkinter` + `ctypes`)만 사용, 런타임 의존성 0 |
| 배포 | PyInstaller 단일 exe (회사 PC에 Python 없음). 프로세스 이름 위장 |
| 화면 영역 | 마우스 커서가 있는 모니터의 작업영역(작업표시줄 제외) 하단 1/3, 가로 전체 |
| 장르 | 메탈슬러그식 런앤건 (이동·점프·사격) |
| 스테이지 규칙 | 좌우 양쪽에서 적이 몰려옴 → 일정 수 처치 후 보스 등장 → 보스 처치 = 클리어 |
| 스테이지 테마 | 회사 부서 10개(일반 부서로 구성, 설정파일로 교체 가능) → 임원진 → CEO → 회장 → 이후 무한(쉬움/보통/하드 로테이션) |
| 적 | 직급 기반. 연구소 스테이지는 연구직 직급 + 흰 가운 |
| 체력 | 1피격 사망, 목숨 3, 사망 시 현재 스테이지 웨이브 처음부터 |
| 캐릭터 | 3명 중 1명 선택. 이재휘(힘) / 문현기(지능) / 석동일(민첩) |
| 스프라이트 | 제공 시트에서 프레임 추출 후 팔레트 스왑으로 3인 + 적 전 직급 생성 |
| 입력 | 게임이 보이는 동안만 키 포커스 점유. 숨기면 이전 창으로 포커스 반환 |
| 핫키 | `Shift+0` 전역 토글(숨김+일시정지 / 복귀+재개). `config.json`에서 변경 가능 |
| 은닉 | 작업표시줄·Alt+Tab 목록에서 제외, exe 이름 위장, 저장파일명 위장 |
| 사운드 | 없음 (코드에 사운드 경로 자체 없음) |
| 저장 | exe 옆 폴더 `cache.dat`(JSON) — 진행 스테이지, 최고점수, 선택 캐릭터, 설정 |

---

## 2. 게임 설계

### 2.1 화면 구성

```
┌─────────────────────────────────────────────────────┐
│  (업무 화면 그대로 보임 — 위 2/3는 게임 창 밖)          │
│                                                     │
├─────────────────────────────────────────────────────┤ ← 작업영역 높이의 2/3 지점
│ [♥♥♥ 12,340점  3스테이지: 인사팀]      [보스 HP ▓▓▓░░] │ ← HUD (반투명 소형)
│         ▂▂▂ 발판                    ▂▂▂▂             │
│  적→  적→      🧍(플레이어)     ←적    ←적            │
│ ═══════════════════════════════════════════════════ │ ← 바닥선 (얇은 도트)
└─────────────────────────────────────────────────────┘ ← 작업표시줄 바로 위
```

- 배경은 완전 투명. 바닥선, 발판, 캐릭터, 탄환, HUD만 그림.
- 투명 영역은 클릭이 아래 창으로 통과(`-transparentcolor` 컬러키 방식, IconCourt 검증됨).
- 스테이지마다 발판 1~2개, 구덩이 0~1개를 `stages.json` 규칙으로 배치.

### 2.2 조작

| 키 | 동작 |
|---|---|
| `←` `→` | 이동 |
| `↑` 또는 `Z` | 점프 |
| `Space` 또는 `X` | 사격 (누르고 있으면 연사) |
| `↓` | 앉기 (낮은 탄 회피) |
| `Shift+0` | **숨기기 / 복귀** (전역, 어느 창에서든) |
| `P` | 일시정지 |
| `Esc` | 종료 (게임 창 포커스 시) |

### 2.3 스테이지 진행

```
캐릭터 선택
  → 1~10 스테이지: 부서 (총무 → 인사 → 재무 → 영업 → 마케팅 → 구매 → 품질 → 생산 → 개발 → 연구소)
  → 11 스테이지: 실장단 (중간보스 3연속)
  → 12 스테이지: 상무·전무
  → 13 스테이지: CEO
  → 14 스테이지: 회장
  → 15~ 무한: 부서 로테이션. 3바퀴마다 쉬움 → 보통 → 하드 (적 속도·수·HP 배율 상승)
```

각 스테이지 = 웨이브 N개(적이 좌우에서 스폰) → 마지막 웨이브 종료 시 보스 등장 → 보스 처치 시 "STAGE CLEAR" 배너 → 다음 스테이지.

### 2.4 적 등급

| 등급 | 직급 | 크기 | HP | 행동 |
|---|---|---|---|---|
| 잡병 | 인턴 < 사원 < 대리 < 과장 | 1x | 1 | 걸어오기, 가끔 정지 사격. 직급 오를수록 속도·사격빈도 증가 |
| 정예 | 차장 < 부장 < 팀장 | 1x | 2~3 | 달리기, 점프, 연사 |
| 중간보스 | 실장 < 상무 < 전무 | 2x | 15~30 | HP바. 돌진 + 탄막 |
| 최종 | CEO, 회장 | 3x | 60 / 100 | 패턴 공격(결재도장 낙하, 좌우 돌진, 부하 소환) |
| 연구소 | 연구원 < 선임 < 책임 < 수석 < 연구소장(보스) | 1x / 2x | 잡병·정예 대응 | 흰 가운 팔레트. 연구소 스테이지 전용 |

부서 스테이지 보스 = 그 부서의 장(팀장 → 부장 → 실장 순으로 후반일수록 상위).
적 머리 위에 직급 라벨 표시(도트 폰트, 예: `대리`).

### 2.5 캐릭터

| 캐릭터 | 사진 | 특성 | 게임 효과 |
|---|---|---|---|
| 이재휘 | 회색 HDEX 티 | 힘 | 탄 데미지 2배(정예 1~2발), 이동속도 0.9배 |
| 문현기 | 남색 티, 엄지척 | 지능 | 관통탄(적 여러 명 한 줄로 처리), 적 HP 표시 |
| 석동일 | 검정 티, 셀카 | 민첩 | 이동 1.3배, 점프 높음, 연사 1.5배 |

이름·스탯은 `config.json`에서 교체 가능(회사에서 실명 노출 원치 않으면 별명으로).

### 2.6 점수·저장

- 처치 점수: 잡병 100 × 직급 계수, 정예 300, 중간보스 2,000, 최종 10,000. 스테이지 클리어 보너스.
- `cache.dat`: `{ "stage": 7, "best": 45200, "char": "이재휘", "hotkey": "shift+0", ... }`
- 게임오버 시 최고점수 갱신. 재실행 시 "이어하기(7스테이지) / 처음부터" 선택.

---

## 3. 기술 설계

### 3.1 파일 구성

```
MolGam/
├─ molgam.py           # 진입점: 창 생성, 루프, 상태머신(선택/플레이/일시정지/게임오버)
├─ overlay.py          # 투명 창, 모니터 판정, 작업표시줄/Alt+Tab 숨김, 전역 핫키, 포커스 반환
├─ game.py             # 순수 로직: 엔티티, 물리, 웨이브, 스테이지, 충돌 (창 없이 --selftest 가능)
├─ sprites.py          # 스프라이트 로딩(내장 base64 PNG → tk.PhotoImage), 애니메이션 정의
├─ assets_data.py      # [생성물] 추출·리컬러된 프레임 PNG를 base64로 내장 (단일 exe용)
├─ stages.json         # 부서 목록, 웨이브 구성, 보스 직급, 발판 규칙 — 코드 수정 없이 교체
├─ config.json         # 핫키, 캐릭터 이름/스탯, 프로세스 위장명
├─ tools/
│   ├─ extract_sprites.py   # 시트 → 프레임 분할 (Pillow, 개발 PC에서만)
│   └─ recolor.py           # 팔레트 스왑 → 3인 + 직급별 적 + 가운 → assets_data.py 생성
├─ run.bat
├─ build_exe.bat       # PyInstaller onefile, 출력명 위장
├─ README.md
└─ PLAN.md (이 문서)
```

### 3.2 오버레이 창 (overlay.py)

- `overrideredirect(True)` + `-topmost` + `-transparentcolor` (IconCourt 그대로).
- 표시 위치: `GetCursorPos` → `MonitorFromPoint` → `GetMonitorInfo().rcWork` → 하단 1/3. 핫키로 복귀할 때마다 재판정.
- 작업표시줄·Alt+Tab 제외: `SetWindowLong(GWL_EXSTYLE, ... | WS_EX_TOOLWINDOW)` 및 `WS_EX_APPWINDOW` 제거.
- 전역 핫키: `RegisterHotKey(hwnd, id, MOD_SHIFT, VK_0)` → tk `after` 루프에서 `PeekMessage(WM_HOTKEY)` 폴링. `config.json`의 `"hotkey": "shift+0"` 문자열 파싱(modifier+key).
- 숨김 시: 직전 `GetForegroundWindow` 저장 → `withdraw()` → 게임 일시정지. 복귀 시: `deiconify()` → `SetForegroundWindow(hwnd)` → 재개. 숨김 상태에서는 게임 창이 어떤 키도 받지 않음(전역 후킹 없음, 숫자 0 정상 타이핑).
- DPI 인식(`SetProcessDpiAwareness`) 적용, 모니터 배율 다른 경우 대비.

### 3.3 게임 루프

- 목표 30fps (`after(33)`), tkinter Canvas. 화면 위 동시 객체 상한 ~40(적 12 + 탄 20 + 이펙트).
- Canvas 아이템 재사용(생성/삭제 대신 `coords`/`itemconfig`)으로 tkinter 부하 최소화.
- 스프라이트 2배 확대(원본 ~40px → 80px). 좌우 반전 프레임은 로딩 시 미리 생성.
- `game.py`는 tk 의존 없음 → `python molgam.py --selftest`로 웨이브·충돌·스테이지 전이 검증.

### 3.4 스프라이트 파이프라인

시트 실측: 1062×1008, 사각 밴드 15줄, 프레임 ~40×45px, 픽셀 격자가 정렬되지 않은 업스케일 이미지(경계 흐림).
따라서 "깨끗한 도트"가 아니라 **양자화 후 색상군 치환** 방식으로 처리.

1. `extract_sprites.py`: 배경(밝은 회백)·격자선(연녹) 마스킹 → 연결요소 → 붙어 있는 프레임은 세로 공백으로 재분할 → 프레임 인덱스 컨택트시트 출력.
2. 애니메이션 셋 수동 매핑(인덱스 → idle / run / jump / shoot / crouch / death / victory). 시트에 모두 존재 확인됨.
3. `recolor.py`: 프레임별 16색 양자화 → 색상군 분류(머리/피부/셔츠/바지/총/외곽선) → 팔레트 적용
   - 플레이어: 셔츠 회색 / 남색 / 검정, 머리 검정, 바지 각각 청바지·회색 반바지·검정
   - 적 잡병·정예: 정장(직급 오를수록 짙은 색), 흰 셔츠
   - 연구소: 흰 가운 + 회색 바지
   - 보스: 정예 팔레트 + NEAREST 2x/3x 확대, 붉은 넥타이
4. 결과를 `assets_data.py`에 base64 PNG로 내장(단일 exe에 파일 동봉 불필요).

### 3.5 은닉

- 창: 제목 없음, 작업표시줄·Alt+Tab 미노출, 트레이 아이콘 없음.
- exe: `build_exe.bat`에서 출력명 지정(기본 `SystemSettingsHelper.exe`, 아이콘 없음).
- 저장: `cache.dat` (JSON이지만 확장자 위장).
- 작업관리자에는 exe 이름으로만 표시됨. 메모리 ~30MB.

---

## 4. 작업 분할 (에이전트 팀 병렬)

| 팀 | 담당 | 산출물 | 독립 검증 |
|---|---|---|---|
| A 에셋 | 프레임 추출, 애니 매핑, 리컬러, 내장 데이터 생성 | `tools/*.py`, `assets_data.py`, 컨택트시트 | 시트 이미지 확인 |
| B 오버레이 | 투명 창, 모니터 판정, 핫키, 포커스 반환, 은닉 스타일 | `overlay.py` | 빈 캔버스로 토글·이동 수동 테스트 |
| C 게임 코어 | 엔티티/물리/웨이브/스테이지/보스 패턴/점수/저장 | `game.py`, `stages.json`, selftest | `--selftest` 통과 |
| D 배포·문서 | run/build 스크립트, README, config 기본값 | `*.bat`, `README.md`, `config.json` | exe 빌드 성공 |
| 통합 | A~D 결합, `molgam.py`, 실기 플레이 확인, 스크린샷 | 동작 exe | 실제 화면에서 3스테이지 플레이 |

A·B·C·D는 서로 인터페이스만 맞추면 동시 진행 가능. 인터페이스는 착수 전 고정:

- `sprites.get(name, variant, frame, flip) -> PhotoImage`
- `overlay.Overlay(on_toggle, on_quit) → .canvas, .rect(x,y,w,h), .show(), .hide()`
- `game.World(stages, config) → .update(dt, keys) / .snapshot()` (그리기는 통합 레이어)

---

## 5. 위험 요소와 대응

| 위험 | 대응 |
|---|---|
| 시트가 흐린 업스케일 → 리컬러 품질 | 16색 양자화 후 치환. 2배 확대 시 가독 확인, 불만족 프레임은 수동 픽셀 보정 |
| `Shift+0` = 영문 자판 `)`. 전역 등록 시 어느 창에서도 `)` 입력 불가 | 사용자 결정 존중. README에 명시, `config.json`으로 즉시 변경 가능(예: `ctrl+shift+0`) |
| tkinter 성능 | 30fps, 객체 상한, 아이템 재사용. 프레임 드랍 시 적 수 자동 축소 |
| PyInstaller exe 백신 오탐 | README 안내. 필요 시 onedir 빌드 대안 |
| 멀티모니터 DPI 차이 | Per-monitor DPI aware 설정, 복귀 시 매번 영역 재계산 |
| 복귀 시 포커스 탈취 실패(Windows 포그라운드 잠금) | `AttachThreadInput` 우회 + 실패 시 창 클릭 안내 |

---

## 6. 다음 단계

1. 이 계획 승인
2. 인터페이스 3개 고정 후 A·B·C·D 병렬 착수
3. 통합 → 실기 플레이 → exe 빌드 → 인수
